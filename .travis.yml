language: node_js

node_js:
  - "10"

cache:
    yarn: true
    directories:
        - node_modules

install:
    yarn install

script:
    yarn run build && yarn run deploy

branches:
    only:
        - master
        - develop
        - /^greenkeeper/.*$/
