language: node_js

node_js:
  - "10"

cache:
    yarn: false
    directories:
        - node_modules

before_install:
  - echo -e "machine github.com\n  login $GITHUB_TOKEN" > ~/.netrc

install:
    npm install

script:
    make release-ci

branches:
    only:
        - master
