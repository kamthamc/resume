language: node_js

node_js:
  - "10"

cache:
    yarn: true
    directories:
        - node_modules

install:
    npm install

script:
    make release-ci

branches:
    only:
        - master
        - develop
        - /^greenkeeper/.*$/
