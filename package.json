{
  "name": "kamthamc-resume",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "dependencies": {},
  "scripts": {
    "prebuild": "lerna clean --yes && lerna bootstrap --ci && lerna link",
    "build": "lerna run build",
    "predeploy": "lerna bootstrap",
    "deploy": "lerna run deploy",
    "prerelease": "lerna bootstrap",
    "release": "lerna run release",
    "lerna": "lerna"
  }
}
