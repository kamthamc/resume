{
	"name": "kamthamc-resume",
	"private": true,
	"devDependencies": {
		"lerna": "^3.14.1"
	},
	"dependencies": {

	},
  "scripts": {
    "build": "lerna run build",
    "deploy": "lerna run deploy",
    "release": "lerna run release"
  }
}
