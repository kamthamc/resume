{
	"name": "kamthamc-resume",
	"private": true,
	"devDependencies": {
		"lerna": "^3.14.1"
	},
	"dependencies": {},
	"scripts": {
		"build": "lerna bootstrap && lerna run build",
		"deploy": "lerna bootstrap && lerna run deploy",
		"release": "lerna bootstrap && lerna run release"
	}
}
